{
  "name": "self",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate": "node --env-file=.env.local scripts/migrate.js"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0"
  }
}
